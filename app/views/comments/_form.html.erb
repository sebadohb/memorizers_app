<!-- コメント入力フォーム ------------------------------------------------------------>
<%= form_with(model: [dictionary, comment], url: dictionary_comments_path(@dictionary),local: true ) do |f| %>
    <%= f.text_area :content, class: "comment-text" %>
    <%= f.submit "submit", class: "comment-btn" %>
<% end %>
